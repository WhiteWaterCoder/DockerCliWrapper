namespace DockerCliWrapper.Docker.Container
{
    public enum ContainerEventStatus
    {
        Unknown,
        Start,
        Stop,
        Die,
        Kill,
        Pause,
        Unpause
    }
}
